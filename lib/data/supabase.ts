import type { DataAdapter } from './types';

// Placeholder. Will be implemented when NEXT_PUBLIC_SUPABASE_URL is set in .env.local.
// Schema lives in supabase/migrations/0001_init.sql.
export const supabaseAdapter: DataAdapter | null = null;
